# pnpm re-runs `install` before every `pnpm run <script>`; that check exits
# non-zero over esbuild's (harmless) blocked postinstall. Skip it — CI installs
# explicitly anyway.
verify-deps-before-run=false
